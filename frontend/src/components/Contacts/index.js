import React, { Component } from "react";

class Contacts extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <>Contacts</>
    );
  }
}

export default Contacts;